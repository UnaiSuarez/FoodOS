{
  "name": "@foodos/engine",
  "version": "0.1.0",
  "private": true,
  "description": "Motores puros de calculo (nutricion, ejercicio, adaptativo) compartidos entre web, movil y escritorio.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "files": [
    "src/**/*.ts",
    "!src/**/*.test.ts"
  ],
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "peerDependencies": {
    "@foodos/types": "*"
  },
  "devDependencies": {
    "@foodos/types": "*",
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0",
    "vitest": "^4.1.11"
  }
}
